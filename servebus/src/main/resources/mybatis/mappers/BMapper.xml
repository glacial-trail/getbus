<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="info.getbus.servebus.dao.BMapper">

	<select id="selectAll" resultType="info.getbus.servebus.model.tmp.B">
        SELECT * FROM b
    </select>
</mapper>