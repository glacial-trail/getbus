<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="info.getbus.servebus.persistence.datamappers.common.CountryMapper">

	<select id="selectAllCodes" resultType="string">
        SELECT code FROM country ORDER BY sequence ASC NULLS LAST
    </select>

</mapper>