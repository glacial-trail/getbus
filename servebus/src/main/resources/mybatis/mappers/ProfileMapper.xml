<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="info.getbus.servebus.persistence.datamappers.user.ProfileMapper">

    <insert id="insert" useGeneratedKeys="false">
        INSERT INTO user_profile(username, time_zone) VALUES
            (#{username}, #{timeZone})
    </insert>

</mapper>